/**
 * Seed wrapper — delegates to the EduCoach seed function.
 * Registered via the auto-discovery system (scans app/modules/<slug>/src/seeds/).
 */
export { seedEduCoach as seedEduCoachData } from "~/educoach/educoach.seed";
